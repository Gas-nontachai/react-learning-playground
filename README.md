# React Learning Playground

แอป Next.js สำหรับเรียน React แบบสองภาษา (ไทย/อังกฤษ) ที่รวมบทเรียนแบบ MDX, ของเล่น Sandpack ที่แก้โค้ดได้ทันที และระบบติดตามความคืบหน้าที่เก็บไว้ในเบราว์เซอร์ ผู้เรียนสามารถสลับภาษา ธีม และให้ระบบอ่านบทเรียนเป็นเสียงได้ในหน้าจอเดียว

## ฟีเจอร์ที่โดดเด่น

- **แพลตฟอร์มสองภาษา** – เส้นทางเรียนทั้งชุดอยู่ใน `src/lessons/th/*.mdx` และ `src/lessons/en/*.mdx` โดย UI แปลด้วย [`next-intl`](src/messages/en.json) ทำให้เปลี่ยนภาษาได้ทั้งตัวบทและอินเทอร์เฟซ
- **บทเรียนพร้อม Playground** – แต่ละบทดึงโค้ดตั้งต้นจาก [`src/lib/lessons-config.ts`](src/lib/lessons-config.ts) ไปเรนเดอร์ใน [`Playground`](src/components/Playground.tsx) ซึ่งฝัง Sandpack (React + TypeScript) พร้อมปุ่ม Reset
- **ติดตามความคืบหน้าบนเครื่อง** – [`useProgress`](src/lib/progress.ts) ใช้ `localStorage` (`progress`) เพื่อจำว่าผู้เรียนทำบทไหนจบแล้วและแสดงผลทั้งในแถบด้านซ้าย, ปุ่ม `ProgressAction`, และสรุปเปอร์เซ็นต์บนหน้าแรก
- **ผู้ช่วยเข้าถึงเนื้อหา** – [`TextToSpeechControl`](src/components/TextToSpeechControl.tsx) ใช้ Web Speech API เพื่ออ่านข้อความตามภาษาที่เลือก, ปุ่มสลับธีมจาก `next-themes`, และแถบค้นหาใน [`Sidebar`](src/components/Sidebar.tsx) ทำให้ UX เป็นมิตรในทุกอุปกรณ์
- **โครงสร้าง SEO ครบถ้วน** – `src/app/layout.tsx` แทรก schema.org `Course` + [`siteMetadata`](src/lib/site-metadata.ts) กำหนด Open Graph/Twitter card และรองรับ `x-default` alternate
- **รองรับ Static Generation** – เส้นทาง `/[locale]/lesson/[slug]` สร้างล่วงหน้าจากข้อมูล `orderedLessons` และ `lessonContent` เพื่อให้โหลดเร็วแม้บทเรียนจำนวนมาก

## Stack & Requirements

- **Runtime**: Node.js ≥ 18.18 (ตาม Next.js 15)
- **Framework**: Next.js 15 App Router + MDX (`@next/mdx`, `@mdx-js/*`)
- **Styling**: Tailwind CSS 3, custom prose stylesใน `mdx-components.tsx`
- **State/UI**: React 18, `next-themes`, `clsx`
- **Content & DX**: Sandpack React (live playground), TypeScript, local scripts ใน `scripts/`

## วิธีเริ่มต้นพัฒนา

```bash
npm install          # ติดตั้ง dependencies
npm run dev          # รัน Next dev server (http://localhost:3000)
npm run lint         # ตรวจ ESLint
npm run build        # สร้าง production build
npm run start        # serve production build
```

> หากต้องการ deploy โปรดตั้ง `NEXT_PUBLIC_SITE_URL` เพื่อให้ [`site-metadata.ts`](src/lib/site-metadata.ts) ประมวลผล canonical URL ที่ถูกต้อง

## โครงสร้างโครงการโดยย่อ

```
src/
  app/                # Next.js App Router รวม layout หลักและ route `[locale]/lesson/[slug]`
  components/         # UI หลัก เช่น Sidebar, LessonLayout, Playground, ThemeToggle
  lessons/            # เนื้อหา MDX แยกสองภาษา + registry.ts
  lib/                # ฟังก์ชันช่วย เช่น i18n, metadata, progress hook, lesson config
  messages/           # ไฟล์แปลข้อความ + label สำหรับ text-to-speech
  styles/             # Tailwind globals
scripts/generate-lessons.mjs  # เครื่องมือแปลง lessons-data เป็นไฟล์ MDX และ registry
mdx-components.tsx    # mapping ของแท็กมาตรฐาน → คลาส Tailwind
```

## สถาปัตยกรรมบทเรียน

1. แกนข้อมูลอยู่ใน [`src/lib/lessons-data.json`](src/lib/lessons-data.json) โดยกำหนด `slug`, `section`, `title`, `summary` รวมถึงลำดับการเรียน
2. ไฟล์ [`src/lib/lessons-config.ts`](src/lib/lessons-config.ts) นำข้อมูลเหล่านี้มาเรียง (`orderedLessons`) และผูกโค้ดตั้งต้นที่ใช้ใน Playground
3. เนื้อหาจริงถูกเก็บเป็น MDX สองภาษาที่ `src/lessons/{th,en}/*.mdx` และอิมพอร์ตผ่าน [`src/lessons/registry.ts`](src/lessons/registry.ts)
4. [`LessonLayout`](src/components/LessonLayout.tsx) เป็น shell ที่รวมบทความ, ปุ่ม Text-to-speech, ปุ่ม mark complete, และ Sandpack

### เพิ่มบทเรียนใหม่

1. เพิ่มรายการใน `src/lib/lessons-data.json` พร้อมรายละเอียดครบ
2. (ทางเลือก) รัน `node scripts/generate-lessons.mjs` เพื่อสร้างไฟล์ MDX โครง (`th/en`) และรีเจน `registry.ts`
3. ใส่โค้ดตัวอย่างใน `lessons-config.ts` (ถ้าต้องการ Playground) และเขียนเนื้อหาในไฟล์ MDX ที่สร้างไว้

## Internationalization & UI

- เส้นทาง `/[locale]` ถูกกำหนดไว้ใน [`src/app/[locale]/layout.tsx`](src/app/%5Blocale%5D/layout.tsx) และ `generateStaticParams`
- ข้อความ UI ทุกจุดจิ้มผ่าน `useTranslations` และได้จาก `src/messages/{th,en}.json`
- ปุ่มสลับภาษา (`LanguageSwitcher`) จะอัปเดต segment แรกของ URL เพื่อให้ `NextIntlClientProvider` ดึงข้อความใหม่ทันที
- `ThemeProvider` (ใน [`Providers.tsx`](src/components/Providers.tsx)) เก็บค่าธีมใน `localStorage` key `rlp-theme`

## Playground, Progress & Accessibility

- [`Playground`](src/components/Playground.tsx) ใช้ Sandpack template `react-ts`, เปิด console + inline errors และแนบ Tailwind CSS CDN เพื่อให้โค้ดตัวอย่างใช้ยูทิลิตี้ที่คุ้นเคย
- ปุ่ม Reset จะรีเซ็ตไฟล์ `/App.tsx` กลับเป็น `lesson.defaultCode` และบังคับ Remount ด้วย `resetKey`
- [`useProgress`](src/lib/progress.ts) เปิดเผย `markCompleted`, `toggleCompleted`, `resetProgress` และใช้ใน Sidebar, ProgressSummary, ProgressAction
- [`TextToSpeechControl`](src/components/TextToSpeechControl.tsx) เลือกเสียงที่ตรงกับ locale (th-TH หรือ en-US) ถ้าไม่พบจะแจ้งเตือนว่าไม่รองรับ

## สคริปต์ที่มี

| คำสั่ง | ทำอะไร |
| --- | --- |
| `npm run dev` | รัน Next dev server |
| `npm run build` | สร้าง production output ใน `.next` |
| `npm run start` | เสิร์ฟไฟล์ที่ build แล้ว |
| `npm run lint` | ตรวจ ESLint ตาม config ของ Next |
| `node scripts/generate-lessons.mjs` | อ่าน `lessons-data.json`, สร้างไฟล์ MDX สองภาษา และอัปเดต `src/lessons/registry.ts` |

## แนวทางต่อยอด

- เชื่อมความคืบหน้ากับ backend หรือบัญชีผู้ใช้เพื่อซิงก์ข้ามอุปกรณ์
- เพิ่ม quiz/checkpoint ต่อบท แล้วเก็บคะแนนในโครงสร้าง `lessons-config`
- รองรับภาษาเพิ่ม โดยสร้างไฟล์ข้อความใหม่ใน `src/messages` และเพิ่ม locale ใน `src/lib/i18n.ts`
- ปรับแต่ง Sandpack ให้รองรับการบันทึก snippet หรือแชร์ลิงก์

โปรเจกต์นี้ออกแบบมาให้เรียนรู้ได้ไว สร้างคอนเทนต์ใหม่ง่าย และยังคง UX ที่เป็นมิตรต่อผู้เริ่มต้น ถ้าพบปัญหาหรืออยากแชร์ไอเดียสามารถเปิด issue/PR ได้เลย 🙌
