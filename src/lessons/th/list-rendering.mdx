# 1.8 List rendering

การแสดงผล array คือหัวใจของ React เราใช้ `Array.map` แปลงข้อมูลเป็น JSX และใช้ `key` เพื่อให้ React รู้ว่ารายการไหนคือรายการใด

<br />

## กฎสำคัญ
- ใส่ `key` ที่นิ่ง เช่น id จากฐานข้อมูล ดีกว่าใช้ index
- ฟังก์ชันใน `map` ควรเป็น pure function ห้าม mutate state หรือเรียก async ภายใน
- เตรียมข้อมูลให้เรียบร้อยก่อนนำไป render เพื่อให้ JSX อ่านง่าย

<br />

## ตัวอย่าง: รายการบทเรียน

```jsx
const lessons = [
  {slug: 'jsx', title: 'JSX basics', level: 'Beginner'},
  {slug: 'state', title: 'Thinking in state', level: 'Beginner'},
  {slug: 'use-effect', title: 'Side effects', level: 'Intermediate'}
];

function LessonList() {
  return (
    <div className="grid gap-4 md:grid-cols-3">
      {lessons.map((lesson) => (
        <article
          key={lesson.slug}
          className="rounded-2xl border border-slate-200 bg-white p-4 shadow"
        >
          <p className="text-xs uppercase text-slate-400">{lesson.level}</p>
          <h3 className="text-lg font-semibold">{lesson.title}</h3>
          <a className="text-sm text-indigo-600" href={`/lessons/${lesson.slug}`}>
            เรียนต่อ →
          </a>
        </article>
      ))}
    </div>
  );
}
```

<br />

## สถานะ loading และ empty
- ระหว่างรอข้อมูลให้แสดง skeleton หรือตัวโหลด
- ถ้าข้อมูลว่าง ให้บอกผู้ใช้อย่างสุภาพ พร้อมแนะนำ action ต่อไป (เช่น “สร้าง todo แรกของคุณ”)

<br />

## Workshop
- Render รายการจากข้อมูลที่คุณใช้จริง (คอร์ส รายการค่าใช้จ่าย เพลย์ลิสต์)
- เพิ่มปุ่มสำหรับ reorder หรือ filter แล้วค่อย map array ที่ผ่านการจัดการแล้ว
- ฝึกการแสดงผลแบบซ้อน (category ด้านนอก มีรายการย่อยด้านใน)

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
