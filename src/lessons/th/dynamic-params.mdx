# 3.2 Dynamic params

Dynamic route ช่วยให้รับค่า segment จาก URL เป็นพารามิเตอร์ เหมาะกับหน้ารายละเอียด เช่น โปรไฟล์ บทเรียน หรือสินค้า

<br />

## โครงสร้างโฟลเดอร์
- `[slug]/page.tsx` → `/lessons/my-first-component`
- หลายพารามิเตอร์: `[course]/lessons/[lesson]/page.tsx`
- แบบจับทุก segment: `[...segments]/page.tsx`

Next.js จะส่ง params ให้ component ผ่าน prop ที่ชื่อ `params`

<br />

## ตัวอย่าง: หน้า lesson

```tsx
// app/lessons/[slug]/page.tsx
import {notFound} from 'next/navigation';
import {lessonContent} from '@/lessons/registry';

const lessons = {
  'react-intro': {title: 'React intro', summary: 'เหตุผลที่มี React'},
  'jsx': {title: 'JSX', summary: 'เขียน UI ใน JavaScript'}
};

type Props = {
  params: {slug: keyof typeof lessons};
};

export default function LessonPage({params}: Props) {
  const lesson = lessons[params.slug];
  if (!lesson) notFound();

  return (
    <article className="space-y-3">
      <p className="text-sm uppercase text-slate-400">Lesson</p>
      <h1 className="text-3xl font-bold">{lesson.title}</h1>
      <p className="text-slate-600">{lesson.summary}</p>
    </article>
  );
}
```

<br />

## เคล็ดลับ
- ตรวจสอบค่า params ก่อนใช้งานเพื่อกันแอปพัง
- ใช้ `generateStaticParams` เพื่อ prerender route ที่มีคนเข้าบ่อยตั้งแต่ build time
- ผสม dynamic route กับ `notFound()` หรือ `redirect()` เพื่อให้ UX ดีขึ้น

<br />

## Workshop
- สร้าง dynamic route `/students/[id]` แล้วดึงข้อมูลโปรไฟล์ตาม id
- ใส่ error handling สำหรับ params ที่ไม่ถูกต้อง (เช่นแสดง 404)
- ทดลองซ้อน dynamic route สำหรับโครงสร้าง course → lesson

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
