# 1.4 Props

Props คือช่องทางที่ component รับข้อมูลจาก component แม่ ทำให้ component เดียวกันเปลี่ยนหน้าตาได้ตาม props ที่ส่งเข้ามา

<br />

## Props ทำงานอย่างไร
1. Component แม่ render `<Greeting name="Beam" mood="curious" />`
2. Component ลูกได้รับ props ในพารามิเตอร์ของฟังก์ชัน
3. Destructure เพื่อให้โค้ดอ่านง่าย และตั้งค่าดีฟอลต์ได้ตามต้องการ

<br />

## ตัวอย่าง: Greeting component

```jsx
type GreetingProps = {
  name: string;
  mood?: 'curious' | 'focused' | 'relaxed';
};

function Greeting({name, mood = 'focused'}: GreetingProps) {
  return (
    <section className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-emerald-900">
      <h3 className="text-lg font-semibold">สวัสดี {name} 👋</h3>
      <p className="text-sm">อารมณ์วันนี้: {mood}</p>
    </section>
  );
}

export default function App() {
  const students = [
    {id: 1, name: 'Mint', mood: 'curious'},
    {id: 2, name: 'Beam', mood: 'relaxed'},
    {id: 3, name: 'Nam'} // ใช้ค่า default
  ];

  return (
    <div className="space-y-3">
      {students.map((student) => (
        <Greeting key={student.id} {...student} />
      ))}
    </div>
  );
}
```

<br />

## แนวปฏิบัติที่ดี
- ใช้ TypeScript (หรือ PropTypes) เพื่อประกาศว่า component คาดหวัง props อะไร
- ส่งเฉพาะข้อมูลที่ component ลูกต้องใช้จริง ไม่ต้องโยน object ใหญ่ ๆ ถ้าใช้แค่ 2 ค่า
- ถ้าชื่อ prop เริ่มกว้างเกินไป เช่น `data` หรือ `item` ให้เปลี่ยนชื่อให้ชัดเจนว่าเป็นข้อมูลอะไร เช่น `lesson`, `todo`, `article`

<br />

## Workshop
- แปลงค่าที่เคย hard-code ใน component ให้รับผ่าน props แล้ว render หลายเวอร์ชัน
- เพิ่ม prop แบบ boolean (เช่น `featured`) เพื่อทดลองสไตล์แบบมีเงื่อนไข
- ส่งฟังก์ชันผ่าน props เพื่อฝึกสถานการณ์ที่ลูกแจ้งแม่ให้เปลี่ยน state
- จดปัญหาเรื่อง props drilling ไว้เพราะเราจะต่อยอดไปบท composition และ context

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
