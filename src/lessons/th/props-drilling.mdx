# 2.5 Props drilling & ทางเลือกอื่น

การส่ง props ผ่าน component หลายชั้นจะลำบากขึ้นเมื่อ tree ใหญ่ขึ้น เราต้องรู้ว่าเมื่อไรควรใช้ props ต่อไป และเมื่อไรควรเปลี่ยนไปใช้ context หรือ composition

<br />

## เมื่อไรที่ props drilling ยังโอเค
- มี component คั่นกลางแค่ 1–2 ชั้น
- ทุกชั้นจำเป็นต้องใช้ props เหล่านั้น (เช่น layout ที่ต้องสไตล์ตามข้อมูล)
- ข้อมูลมีอายุสั้นและใช้เฉพาะสาขานั้น

## เมื่อไรควรมองหาทางเลือก
- มี props หลายตัวที่ไม่เกี่ยวกันต้องส่งให้ลูกลึก ๆ
- component พี่น้องต้องแชร์ state เดียวกันจนยกขึ้นบนแล้วเริ่มยุ่ง
- เห็นชื่อ prop แปลก ๆ เช่น `onChangeFromParent` โผล่หลายไฟล์

<br />

## ตัวอย่าง: ใช้ context สำหรับ theme

```jsx
import {createContext, useContext, useState} from 'react';

type Theme = 'light' | 'dark';

const ThemeContext = createContext<{theme: Theme; toggle: () => void} | null>(null);

function ThemeProvider({children}: {children: React.ReactNode}) {
  const [theme, setTheme] = useState<Theme>('light');
  const toggle = () => setTheme((prev) => (prev === 'light' ? 'dark' : 'light'));

  return (
    <ThemeContext.Provider value={{theme, toggle}}>
      <div className={theme === 'light' ? 'bg-white text-slate-900' : 'bg-slate-900 text-white'}>
        {children}
      </div>
    </ThemeContext.Provider>
  );
}

function ThemeToggleButton() {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error('Use inside ThemeProvider');

  return (
    <button onClick={ctx.toggle} className="rounded-full border px-3 py-1">
      สลับเป็นโหมด {ctx.theme === 'light' ? 'dark' : 'light'}
    </button>
  );
}

export default function App() {
  return (
    <ThemeProvider>
      <div className="space-y-4 p-6">
        <h1>Props Drilling Alternative</h1>
        <ThemeToggleButton />
      </div>
    </ThemeProvider>
  );
}
```

<br />

## เคล็ดลับ
- เริ่มจาก props เสมอ แล้วค่อยใช้ context หรือ state library เมื่อเจอปัญหาจริง
- สร้าง custom hook เพื่อซ่อนรายละเอียดการใช้ context และช่วยลดการส่ง props
- ใช้ composition (ส่ง JSX) เพื่อตัดความจำเป็นของ callback บางตัวออกไปเลย

<br />

## Workshop
- วาด component tree ของคุณแล้วไฮไลต์ props ที่เดินทางมากกว่า 2 ชั้น
- รีแฟกเตอร์แขนงหนึ่งให้ใช้ context แล้วเปรียบเทียบโค้ดก่อน-หลัง
- ทดลองใช้ render prop แทน callback สำหรับสถานการณ์ที่โต้ตอบซับซ้อน

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
