# 1.3 Component แรก

Component คือฟังก์ชัน JavaScript ที่รีเทิร์น JSX เฉย ๆ ให้คิดว่าเป็นตัวต่อ LEGO หนึ่งชิ้น ที่ยิ่งเล็ก ยิ่งดูแลง่าย และเอาไปประกอบชิ้นอื่นได้สะดวก

<br />

## คุณสมบัติของ component ที่ดี
- มีหน้าที่เดียวชัดเจน เช่น `ProfileCard` ไว้แสดงโปรไฟล์คนเดียว
- รับข้อมูลผ่าน props แทนการไปดึงค่าจากที่อื่นแบบสุ่ม
- เก็บเฉพาะโค้ดที่จำเป็น ทั้ง markup และ helper เล็ก ๆ ภายใน

<br />

## ตัวอย่าง: สร้างโปรไฟล์การ์ด

```jsx
function ProfileCard() {
  const name = "Mint";
  const role = "Frontend Student";
  const skills = ["React", "CSS", "Figma"];

  return (
    <article className="rounded-2xl border border-slate-200 bg-white p-6 shadow">
      <header className="flex items-center gap-4">
        <div className="size-14 rounded-full bg-gradient-to-br from-indigo-400 to-purple-500" />
        <div>
          <h2 className="text-xl font-semibold">{name}</h2>
          <p className="text-sm text-slate-500">{role}</p>
        </div>
      </header>
      <ul className="mt-4 flex flex-wrap gap-2 text-sm text-slate-600">
        {skills.map((skill) => (
          <li key={skill} className="rounded-full bg-slate-100 px-3 py-1">
            {skill}
          </li>
        ))}
      </ul>
    </article>
  );
}
```

component เดียวนี้มีตัวแปรภายใน JSX และการ render list เหมือนในโปรเจกต์จริงทุกประการ

<br />

## เคล็ดลับการตั้งชื่อและจัดไฟล์
- ตั้งชื่อแบบ PascalCase: `LessonList`, `Sidebar`
- ช่วงเริ่มต้นให้มี 1 component ต่อไฟล์ ถ้าไฟล์ยาวเกิน ~100 บรรทัดค่อยแยกใหม่
- อย่าลืม export component เพื่อให้ไฟล์อื่น import ได้: `export default function ProfileCard() { ... }`

<br />

## Workshop
- ลองสร้าง UI ที่ใช้อยู่ประจำ (เช่นการ์ดเพลง Spotify, รายการวิดีโอ YouTube) ให้เป็น component
- ถ้าเห็น markup ซ้ำ ๆ ให้ย้ายออกเป็น component ย่อยแล้ว import มาใช้
- ทดลองเพิ่มคลาส Tailwind หรือ CSS เพื่อฝึกจัด layout และสไตล์
- จดประเด็นที่ยังงงแล้วคุยกับเพื่อนหรือเมนเทอร์เพื่อเคลียร์ความเข้าใจ

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
