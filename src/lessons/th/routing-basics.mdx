# 3.1 Routing basics (Next.js App Router)

Next.js ใช้โครงสร้างโฟลเดอร์เป็นตัวกำหนดเส้นทาง ทุกโฟลเดอร์ใน `app/` จะถูกแปลงเป็น URL และไฟล์ `page.tsx` คือ UI ของเส้นทางนั้น

<br />

## แนวคิดหลัก
- `app/page.tsx` → `/`
- `app/lessons/page.tsx` → `/lessons`
- `layout.tsx` ในแต่ละโฟลเดอร์จะห่อ route ลูก ๆ เพื่อแชร์ UI เช่นแถบนำทางหรือฟอนต์
- ใช้ `Link` จาก `next/link` เพื่อให้ navigation แบบ client-side

<br />

## โครงสร้างตัวอย่าง
```
app/
├── layout.tsx        // เปลือกหลักของแอป
├── page.tsx          // หน้าแรก
├── lessons/
│   ├── layout.tsx    // Layout ของหน้า lessons
│   └── page.tsx      // /lessons
└── about/
    └── page.tsx      // /about
```

### ตัวอย่าง layout/page
```tsx
// app/lessons/layout.tsx
import Link from 'next/link';

export default function LessonsLayout({children}: {children: React.ReactNode}) {
  const items = [
    {href: '/lessons/react-intro', label: 'React intro'},
    {href: '/lessons/jsx', label: 'JSX'}
  ];

  return (
    <div className="grid gap-6 md:grid-cols-[200px_1fr]">
      <aside className="space-y-2 rounded-2xl border border-slate-200 p-4">
        {items.map((item) => (
          <Link key={item.href} href={item.href} className="block rounded px-3 py-2 hover:bg-slate-100">
            {item.label}
          </Link>
        ))}
      </aside>
      <main className="rounded-2xl border border-slate-200 p-6 bg-white">{children}</main>
    </div>
  );
}

// app/lessons/page.tsx
export default function LessonsPage() {
  return <h1>เลือกบทเรียน</h1>;
}
```

<br />

## เคล็ดลับ
- พยายามจัดโครงสร้างให้แบน ใช้โฟลเดอร์ซ้อนเฉพาะกรณีที่ต้องการ layout ชั้นในจริง ๆ
- ตั้งชื่อโฟลเดอร์ให้ตรงกับ URL เพื่อให้ทีมเข้าใจได้ทันทีว่าอยู่หน้าไหน
- เก็บ component เฉพาะหน้ารวมไว้ในโฟลเดอร์ของ route เพื่อลดความสับสน

<br />

## Workshop
- เพิ่ม route ใหม่ สร้าง layout ของมัน และลิงก์ใน navigation
- ย้ายหน้าใดหน้าหนึ่งไปอยู่ใน layout ของตัวเองเพื่อใช้ breadcrumb หรือ sidebar ร่วมกัน
- เปิด DevTools ดู URL ที่เกิดขึ้นจริง เพื่อยืนยันว่าโครงสร้าง routing ถูกตามตั้งใจ

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
