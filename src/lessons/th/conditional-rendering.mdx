# 1.7 Conditional rendering

Component มักต้องแสดงผลต่างกันตาม state เพื่อให้ UI ตรงกับข้อมูลจริง เราจึงต้องใช้ conditional rendering

<br />

## รูปแบบที่ใช้บ่อย
- ใช้ `if` ก่อน `return` เพื่อ handle เคสอย่าง loading หรือ redirect
- ใช้ ternary สำหรับข้อความหรือ element แบบสั้น `{isOnline ? 'Online' : 'Offline'}`
- ใช้ AND operator เพื่อแสดงเฉพาะบางส่วน `{hasError && <ErrorBanner />}`

<br />

## ตัวอย่าง: เลือก badge

```jsx
import {useState} from 'react';

const badges = ['Beginner', 'Builder', 'Deployer'];

export default function BadgeSelector() {
  const [level, setLevel] = useState<string | null>(badges[0]);

  const message = level
    ? `Current badge: ${level}`
    : 'Pick a badge to continue';

  return (
    <div className="space-y-4 rounded-2xl border border-blue-200 bg-white p-6">
      <div className="flex gap-3">
        {badges.map((badge) => (
          <button
            key={badge}
            onClick={() => setLevel(level === badge ? null : badge)}
            className={[
              'rounded-full border px-3 py-1 text-sm',
              badge === level
                ? 'border-blue-600 bg-blue-600 text-white'
                : 'border-slate-300 text-slate-600'
            ].join(' ')}
          >
            {badge}
          </button>
        ))}
      </div>

      <p className="text-sm text-slate-600">{message}</p>

      {level === 'Deployer' && (
        <div className="rounded-lg border border-amber-200 bg-amber-50 p-3 text-amber-900">
          🚀 เคล็ดลับ: ก่อน deploy ตรวจสอบ environment variables ให้ครบ
        </div>
      )}
    </div>
  );
}
```

<br />

## เคล็ดลับ
- ถ้า JSX ในเงื่อนไขเริ่มอ่านยาก ให้แยกเป็น component ย่อย
- อย่าใช้ ternary ซ้อนลึก ๆ ควรใช้ `if` คำนวณตัวแปรกลางแทน
- อย่าลืมเตรียม empty state เพื่อให้ผู้ใช้ไม่เจอหน้าว่างเปล่า

<br />

## Workshop
- ใส่ loading, empty, success, error state ให้ component ที่ดึงข้อมูล
- ทดลอง toggle ส่วนต่าง ๆ ด้วย boolean หรือ state ที่ derive
- ไล่ดู design system ว่ามี component ไหนต้องเปลี่ยนสไตล์ตามสถานะบ้าง

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
