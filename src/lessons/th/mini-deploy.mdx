# 4.6 Mini project – deploy

นำ mini project ขึ้นออนไลน์เพื่อดูประสิทธิภาพจริงและเก็บ feedback

<br />

## Checklist ก่อน deploy
- รัน `npm run lint` และ `npm run test` (ถ้ามี)
- เติมค่า `.env.production` สำหรับ API key แล้วใส่ใน `.gitignore`
- อัปเดต `README.md` ให้มีขั้นตอน deploy ชัดเจน

<br />

## Deploy ขึ้น Vercel
1. push โปรเจกต์ขึ้น GitHub
2. สร้างโปรเจกต์ใหม่บน Vercel แล้ว import repo
3. ตั้ง environment variables ใน **Settings → Environment Variables**
4. กด deploy production และทดสอบผลลัพธ์

<br />

## หลัง deploy
- คลิกทุกหน้าตรวจ routing และ data fetching ว่าทำงานบน server จริง
- เปิดดู log ผ่าน dashboard ของ Vercel เพื่อจับ runtime error
- ส่งลิงก์ให้เพื่อนลองใช้งานแล้วเก็บ feedback เรื่อง UX

<br />

## Workshop
- ตั้ง preview deployment ให้ทุก pull request
- เพิ่ม status badge หรือปุ่ม “View deployment” ใน repo
- ทดลอง rollback ไป deployment เก่าหากมีบั๊กโผล่

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
