# 4.4 Mini project – component reuse

เมื่อเชื่อมข้อมูลแล้ว ให้มองหา pattern UI ที่ซ้ำกันเพื่อรีแฟกเตอร์เป็น component ใช้ซ้ำ

<br />

## ขั้นตอน
1. สำรวจหน้าจอว่ามี markup ไหนซ้ำ (card, list item, stat)
2. แยก block เหล่านั้นไปไว้ใน `src/components`
3. รับ props เพื่อเปลี่ยนสไตล์ เช่น `status`, `tone`, `size`

<br />

## ตัวอย่าง: Stat widget

```tsx
type StatProps = {
  label: string;
  value: string;
  trend?: 'up' | 'down';
};

export function Stat({label, value, trend}: StatProps) {
  const trendColor = trend === 'up' ? 'text-emerald-600' : 'text-rose-600';
  const trendIcon = trend === 'up' ? '▲' : '▼';

  return (
    <div className="rounded-2xl border border-slate-200 bg-white p-4 shadow">
      <p className="text-sm text-slate-500">{label}</p>
      <p className="text-3xl font-semibold">{value}</p>
      {trend && <p className={`text-sm ${trendColor}`}>{trendIcon} 12% เทียบสัปดาห์ก่อน</p>}
    </div>
  );
}
```

นำ widget นี้ไปใช้ได้ทุกที่เพียงส่ง props ต่างกัน

<br />

## Workshop
- เลือกหน้าหนึ่งแล้วลด JSX ที่ซ้ำลงให้ได้อย่างน้อย 50%
- สร้าง storybook หรือหน้า docs เล็ก ๆ แสดง component ที่ใช้ซ้ำทั้งหมด
- รองรับ visual variant (success, warning, neutral) โดยไม่ต้อง duplication markup

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
