# 4.3 Mini project – fetch data จริง

เชื่อม layout ของคุณกับแหล่งข้อมูลจริง เพื่อให้กราฟและรายการมีความหมาย

<br />

## ทางเลือก
- ใช้ mock data จากบท setup แล้วเปิดเป็น API ผ่าน `app/api/*.ts`
- เรียก API ภายนอก เช่น Supabase, Firebase, JSONPlaceholder
- ถ้าข้อมูลอ่อนไหว ให้ fetch ใน Server Component แล้วส่งลงเป็น props

<br />

## ตัวอย่าง API route
```ts
// src/app/api/students/route.ts
import {NextResponse} from 'next/server';
import {students} from '@/lib/data';

export async function GET() {
  return NextResponse.json(students);
}
```

### ตัวอย่าง client component
```tsx
'use client';
import useSWR from 'swr';

const fetcher = (url: string) => fetch(url).then((res) => res.json());

export function StudentsList() {
  const {data, error, isLoading} = useSWR('/api/students', fetcher);

  if (isLoading) return <p>Loading...</p>;
  if (error) return <p>โหลดรายชื่อนักเรียนไม่สำเร็จ</p>;

  return (
    <ul className="space-y-3">
      {data.map((student: any) => (
        <li key={student.id} className="rounded-xl border border-slate-200 p-4">
          <div className="flex items-center justify-between">
            <p className="font-medium">{student.name}</p>
            <span className="text-sm text-slate-500">{student.progress}%</span>
          </div>
        </li>
      ))}
    </ul>
  );
}
```

<br />

## Workshop
- ผูกอย่างน้อยสองส่วนของ dashboard กับข้อมูลจริง
- ใส่ UI สวย ๆ สำหรับสถานะ loading/error
- แคชผลลัพธ์ด้วย SWR/React Query หรือ fetch ฝั่ง server สำหรับหน้าที่ SSR

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
