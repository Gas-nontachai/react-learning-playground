# 2.4 Component Composition

Composition คือการประกอบ component เล็กหลาย ๆ ชิ้นให้เป็น UI ที่ซับซ้อน แทนการสร้าง component ใหญ่ที่ทำทุกอย่าง ให้แยกหน้าที่แล้วนำมาประกอบกัน

<br />

## รูปแบบ composition ที่เจอบ่อย
- **Layout component** รับผิดชอบเรื่อง spacing หรือกริด แล้วสอด children เข้าไป
- **Slot-based component** รับ JSX ผ่าน props เช่น `header`, `actions`
- **Children แบบ function** (render props) สำหรับเคสที่ต้องควบคุมการ render รายละเอียด

<br />

## ตัวอย่าง: Dashboard cards

```jsx
type CardProps = {
  title: string;
  description: string;
  actions?: React.ReactNode;
  children: React.ReactNode;
};

function DashboardCard({title, description, actions, children}: CardProps) {
  return (
    <section className="space-y-3 rounded-2xl border border-slate-200 bg-white p-5 shadow">
      <header className="flex items-start justify-between gap-3">
        <div>
          <h3 className="text-lg font-semibold">{title}</h3>
          <p className="text-sm text-slate-500">{description}</p>
        </div>
        {actions}
      </header>
      {children}
    </section>
  );
}

export default function Dashboard() {
  return (
    <div className="grid gap-4 md:grid-cols-2">
      <DashboardCard
        title="Students"
        description="Active learners this week"
        actions={<button className="text-sm text-indigo-600">View all</button>}
      >
        <p className="text-4xl font-bold">48</p>
      </DashboardCard>

      <DashboardCard
        title="Feedback"
        description="Latest comments"
        actions={<button className="text-sm text-slate-600">Mark read</button>}
      >
        <ul className="space-y-2 text-sm text-slate-600">
          <li>“Loved the JSX exercise!”</li>
          <li>“Props lesson clarified a lot.”</li>
        </ul>
      </DashboardCard>
    </div>
  );
}
```

<br />

## เคล็ดลับ
- เปิดช่อง `children` ให้ component เพื่อให้ผู้ใช้ตัดสินใจว่าจะใส่อะไรด้านใน
- ลด props drilling ด้วยการวาง context provider ใกล้ ๆ layout หลักแล้วส่งค่าผ่าน composition
- ถ้า children ธรรมดายังไม่พอ ใช้ render props ให้ caller ควบคุมการ render ได้เต็มที่

<br />

## Workshop
- ย้ายส่วน UI ที่ซ้ำ ๆ ให้กลายเป็น layout component ที่ใช้ได้หลายหน้า
- เพิ่ม slot (`header`, `footer`, `avatar`) ให้ card แล้วลองส่ง children ต่างกัน
- ทดลองใช้ render props สำหรับ dropdown, tooltip หรือส่วนที่ต้องตัดสินใจตอน render

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
