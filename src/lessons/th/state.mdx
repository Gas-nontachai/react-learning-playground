# 1.5 State & rerender

State เก็บข้อมูลที่เปลี่ยนไปตามเวลา (ข้อความใน input, toggle, จำนวนคลิก) เมื่อเรียกฟังก์ชัน setter React จะ rerender component พร้อมค่าล่าสุด

<br />

## พื้นฐาน useState
```jsx
const [value, setValue] = useState(initialValue);
```
- การ render ครั้งแรกใช้ `initialValue`
- `setValue` จะสั่งให้ React rerender พร้อมค่าถัดไป
- React รวม (batch) การอัปเดตหลายอันที่เกิดใน event เดียว เพื่อประหยัดแรง render

<br />

## ตัวอย่าง: Focus Tracker

```jsx
import {useState} from 'react';

export default function FocusTracker() {
  const [count, setCount] = useState(0);
  const [topic, setTopic] = useState('React fundamentals');

  return (
    <div className="space-y-6 rounded-2xl border border-purple-200 bg-white p-6 shadow">
      <section>
        <p className="text-sm uppercase tracking-wide text-purple-500">Deep work sessions</p>
        <p className="text-5xl font-bold text-purple-800">{count}</p>
        <button
          className="mt-3 rounded-full bg-purple-600 px-4 py-2 text-white"
          onClick={() => setCount((c) => c + 1)}
        >
          +1 session
        </button>
      </section>

      <section>
        <label className="text-sm text-slate-500">หัวข้อที่กำลังโฟกัส</label>
        <input
          value={topic}
          onChange={(event) => setTopic(event.target.value)}
          className="mt-2 w-full rounded-lg border border-slate-200 p-2"
        />
        <p className="mt-2 text-slate-600">ตอนนี้กำลังโฟกัส {topic}</p>
      </section>
    </div>
  );
}
```

<br />

## เคล็ดลับ
- ห้ามแก้ state ตรง ๆ (`count++`) ต้องสร้างค่าชุดใหม่แล้วส่งให้ setter
- ถ้าค่าถัดไปพึ่งพาค่าก่อนหน้า ใช้รูปแบบฟังก์ชัน: `setCount((prev) => prev + 1)`
- เก็บ state ให้น้อยที่สุด ถ้าค่าคำนวณได้จาก state/props อื่นให้ derive เอา

<br />

## Workshop
- แปลง UI ที่นิ่ง ๆ ให้โต้ตอบได้ด้วยการเพิ่ม state อย่างน้อยสองตัว
- ลอง console.log ภายใน component หรือเพิ่มเอฟเฟกต์ CSS เพื่อดูจังหวะ rerender
- แยก component ใหญ่เป็นส่วนย่อย เพื่อให้เฉพาะส่วนที่มี state rerender

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
