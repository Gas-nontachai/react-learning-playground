# 3.3 Not Found page

หน้า 404 ที่ดีช่วยให้ผู้ใช้หาทางกลับได้เมื่อเข้า route ที่ไม่มีอยู่ ใน App Router เราสร้าง `not-found.tsx` ภายในโฟลเดอร์ route ใดก็ได้

<br />

## การทำงาน
- เรียก `notFound()` ใน `page.tsx` เพื่อให้แสดง `not-found.tsx` ของโฟลเดอร์นั้น
- วาง `not-found.tsx` ที่ `app/` เพื่อเป็น fallback ของทั้งแอป
- ใส่ลิงก์กลับหน้า home หรือช่องค้นหาเพื่อให้ผู้ใช้เริ่มใหม่ได้

<br />

## ตัวอย่าง

```tsx
// app/lessons/not-found.tsx
import Link from 'next/link';

export default function LessonsNotFound() {
  return (
    <div className="rounded-2xl border border-slate-200 bg-white p-8 text-center">
      <p className="text-sm uppercase tracking-wide text-slate-400">404</p>
      <h1 className="text-3xl font-bold">ไม่พบบทเรียน</h1>
      <p className="mt-2 text-slate-600">
        บทเรียนที่ค้นหาอาจถูกย้ายชื่อ ลองเลือกบทอื่นจากรายการดูนะ
      </p>
      <Link
        href="/lessons"
        className="mt-4 inline-flex rounded-full bg-slate-900 px-4 py-2 text-white"
      >
        กลับหน้าบทเรียน
      </Link>
    </div>
  );
}
```

<br />

## เคล็ดลับ
- บอกให้ชัดว่าเกิดอะไรขึ้น พร้อมเสนอทางออก (ค้นหา ติดต่อทีมงาน หรือกลับหน้าหลัก)
- เก็บ log 404 ถ้าเกิดบ่อย เพื่อปรับ redirect และเนื้อหา
- ใช้น้ำเสียงเป็นกันเอง เพิ่มบุคลิกเล็กน้อยเพื่อให้ประสบการณ์ดีขึ้น

<br />

## Workshop
- สร้างหน้า 404 สำหรับแขนงหนึ่งของ route แล้วลอง trigger ด้วย `notFound()`
- เพิ่มระบบ analytics เพื่อติดตาม URL ที่หาย
- ออกแบบส่วน “บทเรียนแนะนำ” หรือ “เพิ่งเปิดล่าสุด” เพื่อให้ผู้ใช้ไม่หลุดออกจากแอป

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
