# 1.6 Events

Event ทำให้ component ตอบสนองต่อการกระทำของผู้ใช้ เช่น คลิก กดคีย์บอร์ด ส่งฟอร์ม ใน React เราติด handler กับ element ใน JSX ได้โดยตรง

<br />

## โครงสร้าง event handler
```jsx
function Button() {
  const handleClick = () => {
    console.log('Clicked!');
  };

  return <button onClick={handleClick}>Click</button>;
}
```
- ฟังก์ชัน handler อยู่ใน component เพื่อให้เข้าถึง state/props ได้
- React ช่วย normalize event ให้ทำงานเหมือนกันทุกเบราว์เซอร์

<br />

## ตัวอย่าง: กดเช็ก todo

```jsx
import {useState} from 'react';

type Todo = {
  id: number;
  text: string;
  done: boolean;
};

export default function TodoList() {
  const [todos, setTodos] = useState<Todo[]>([
    {id: 1, text: 'Review JSX', done: false},
    {id: 2, text: 'Practice props', done: true}
  ]);

  const toggleTodo = (id: number) => {
    setTodos((items) =>
      items.map((item) =>
        item.id === id ? {...item, done: !item.done} : item
      )
    );
  };

  return (
    <ul className="space-y-2">
      {todos.map((todo) => (
        <li
          key={todo.id}
          className="flex items-center gap-3 rounded border border-slate-200 p-3"
        >
          <button
            onClick={() => toggleTodo(todo.id)}
            className={[
              'size-6 rounded-full border',
              todo.done
                ? 'border-emerald-600 bg-emerald-500'
                : 'border-slate-400'
            ].join(' ')}
          />
          <span className={todo.done ? 'text-slate-400 line-through' : ''}>
            {todo.text}
          </span>
        </li>
      ))}
    </ul>
  );
}
```

<br />

## เคล็ดลับ
- ถ้าต้องหยุดพฤติกรรมเดิมของเบราว์เซอร์ให้เรียก `event.preventDefault()` ใน handler
- ตั้งชื่อ handler ให้สื่อหน้าที่ เช่น `handleSubmit`, `handleFilterChange`
- การใช้ arrow function inline (`onClick={() => toggle(id)}`) ไม่ผิด เหมาะกับกรณีที่ logic สั้น ถ้ายาวให้แยกออกมา

<br />

## Workshop
- สร้างปุ่มหลายแบบที่อัปเดต state คนละอัน (toggle dark mode, สลับข้อมูล เป็นต้น)
- เพิ่ม event คีย์บอร์ดให้ input (`onKeyDown`, `onChange`) แล้ว log ค่า
- ทดลองใช้ `preventDefault` ในการ submit form และจัดการส่งข้อมูลเอง

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
