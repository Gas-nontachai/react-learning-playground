# 5.2 Project structure tips

โปรเจกต์ขนาดใหญ่มักโตเร็ว ควรวางมาตรฐานเพื่อให้ทุกคนรู้ว่าควรเพิ่มโค้ดที่ไหนโดยไม่ทำให้โครงสร้างเละ

<br />

## เลย์เอาต์เป็นชั้น ๆ
- **app/** – routing และ entry point ที่ render ฝั่ง server
- **modules/** – รวมโค้ดตามโดเมน (auth, billing, lessons)
- **components/** – UI พื้นฐานที่แชร์ได้ทั่วแอป
- **lib/** – utility กลาง เช่น fetcher, analytics, feature flag
- **types/** – คำอธิบาย TypeScript ที่ใช้ร่วมกัน

<br />

## ตัวอย่าง feature module
```
src/modules/lessons/
├── api/
│   └── getLessons.ts
├── components/
│   ├── LessonList.tsx
│   └── LessonFilters.tsx
├── hooks/
│   └── useLessonFilters.ts
└── index.ts
```

เผยแพร่เฉพาะของที่ module อื่นต้องใช้ผ่าน `index.ts`

<br />

## เอกสารประกอบ
- ทำ README สั้น ๆ อธิบายหน้าที่ของแต่ละโฟลเดอร์
- ใช้ code owners เพื่อขอรีวิวอัตโนมัติในส่วนที่สำคัญ
- ตั้ง lint rule (ลำดับ import, path alias) เพื่อบังคับโครงสร้าง

<br />

## Workshop
- รีแฟกเตอร์โดเมนหนึ่งให้เป็น module พร้อม entry point ของตัวเอง
- เพิ่ม path alias (`@/modules/lessons`) เพื่อให้ import สั้นลง
- จดมาตรฐานการตั้งชื่อ (ลงท้าย component, ขึ้นต้น hook) ลงใน CONTRIBUTING.md

> 💡 ใช้ Playground ด้านขวาเพื่อทดลองโค้ดและรีเซ็ตได้ตลอดเวลา.
